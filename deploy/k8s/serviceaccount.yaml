# ServiceAccount the sf2loki pod runs as. It carries no cluster permissions by
# itself — the Role/RoleBinding in rbac.yaml grant it exactly what the
# k8s_lease coordinator needs (get/create/update on Leases in this namespace).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sf2loki
  namespace: sf2loki
